name: Smoke Reminder

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  send-reminder:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install requests

      - name: Send smoke reminder
        env:
          SMOKE_WEBHOOK: ${{ secrets.SMOKE_WEBHOOK }}
        run: python smoke_reminder.py
